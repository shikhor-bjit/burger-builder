import React from 'react';

const OrderList = props => {
    return (
        <div className={'OrderList'}>
            <p>Order List</p>
        </div>
    );
};

OrderList.propTypes = {};

export default OrderList;