import './Spinner.css';
import React from 'react';

const Spinner = props => {
    return (
        <div className={'Spinner'}>Loading....</div>
    );
};

Spinner.propTypes = {};

export default Spinner;